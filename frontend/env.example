# NoteSmith Frontend Environment Configuration
# Copy this file to .env.local and fill in your values

# Supabase Configuration
# Find these in: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Publishable Key (anon) - safe for client-side use
# Dashboard label: "anon" under "Project API keys" or "publishable" under "API keys"
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-publishable-key

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================
# 
# ⚠️  BEFORE PRODUCTION: Re-enable email confirmation!
#     Supabase Dashboard → Authentication → Providers → Email → "Confirm email"
#     
#     Currently DISABLED for development convenience.
#     This is a SECURITY requirement for production deployments.
# =============================================================================
